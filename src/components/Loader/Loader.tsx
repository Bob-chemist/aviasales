import React, { FC } from 'react';
import './Loader.sass';

const Loader: FC = () => {
  return (
    <div className="Loader-container">
      <div className="Loader" />
    </div>
  );
};

export default Loader;
