import React from 'react';
import './Loader.sass';

const Loader = () => {
  return (
    <div className="Loader-container">
      <div className="Loader" />
    </div>
  );
};

export default Loader;
